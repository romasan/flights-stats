# Копия этого файла — пример настройки окружения.
# Скопируйте его в .env и при необходимости измените значения.
# Файл .env не попадает в git (см. .gitignore).

# Порт HTTP-сервера (по умолчанию 3000)
PORT=3000
